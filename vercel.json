{
    "version": 2,
    "builds": [
      {
        "src": "package.json",
        "use": "@vercel/next",
        "config": {
          "installCommand": "npm install --prefer-offline",
          "buildCommand": "npm run build"
        }
      }
    ]
  }